// src/services/authService.js
// This is a mock file to allow the frontend to build
// Authentication should be handled by the backend server

export const getAuthenticatedClient = async () => {
  console.warn('getAuthenticatedClient called on frontend - authentication should be done on backend');
  return null;
};

export const getAccessToken = async () => {
  console.warn('getAccessToken called on frontend - authentication should be done on backend');
  return null;
};
